json.extract! section, :id, :section_id, :section_number, :course_id, :catalog_number, :term_id, :campus, :days_of_week, :time_start, :time_end, :location, :instructor_name, :instructor_email, :current_num_required_graders, :num_required_graders, :created_at, :updated_at
json.url section_url(section, format: :json)
